package pl.infoshare.model;

public class UserInputItem extends ItemComponent {
    public UserInputItem(String name, Category category, Producer producer) {
        super(name, category, producer);
    }
}
